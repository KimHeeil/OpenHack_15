<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.openhack">

    <uses-sdk tools:overrideLibrary="android.support.*" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="theme, icon, label">
        <activity android:name=".scrapPage"></activity>
        <activity android:name=".jobDetailPage" />
        <activity android:name=".registerPage3" />
        <activity android:name=".registerPage2" />
        <activity android:name=".registerPage" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA-lINJn4ypbhmu5sJby6gqsOo8lVMaGsY" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".listPage" />
    </application>

</manifest>